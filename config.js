const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "sislur",
    password: "smriti*393",
    database: "topsearchengines",
    multipleStatements: true
  },
  listPerPage: 10,
};

module.exports = config;
